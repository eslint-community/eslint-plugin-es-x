---
"eslint-plugin-es-x": major
---

Drop legacy config support. Configs without the `flat/` prefix now expose flat config objects, and the `flat/`-prefixed config IDs remain available as flat config aliases.
